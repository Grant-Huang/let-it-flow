{
  "name": "@let-it-flow/ai-content-factory",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx server/index.ts",
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config ../../vitest.config.ts tests/"
  },
  "dependencies": {
    "let-it-flow": "workspace:*"
  }
}
